import ExamManager from "@/features/main/exams/components/ExamManager";

export const metadata = {
    title: "Learning Management System",
    description: "Take your exams and track your progress.",
};

export default function ExamPage() {
    return <ExamManager />;
}
